<?xml version="1.0" encoding="UTF-8"?>
<project name="UVa" default="run">

    <description>Used to run local tests in IDEA</description>

    <property name="build-dir" value="classes" />

    <target name="init">
        <mkdir dir="${build-dir}" />
    </target>

    <target name="cleanup">
        <delete dir="${build-dir}" />
    </target>

    <target name="run">
        <echo> -- Run! -- </echo>
    </target>

</project>